# Final Import Trigger

The source importer now excludes workflow files so the runner can publish the verified source tree without requiring the separate workflows permission. The CMake workflow will be added through the connected GitHub integration after the source commit lands.
