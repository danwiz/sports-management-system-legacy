# v0.11.0 Increment 1 — Service-Routed Lifecycle Operations

This increment completes the first `v0.11.0` roadmap item.

## Implemented

- Added `SportsService::remove_member` with performance-reference protection.
- Added `SportsService::remove_match` with performance-reference protection.
- Added validated performance update and removal operations.
- Consolidated performance validation for add and update operations.
- Routed CLI member and match deletion through `SportsService`.
- Added CLI performance edit and delete workflows through `SportsService`.
- Added regression coverage proving rejected mutations leave stored data unchanged.

## Validation target

The CMake workflow must configure, compile, and run all test suites successfully before Issue #10 is closed.
